# Local environment overrides
# Copy this file to .env.local and modify as needed
# .env.local is git-ignored

VITE_API_BASE_URL=http://localhost:9999

# WeChat website login credentials, callback URLs, scope, and allowlist are backend-only.
# Do not add AppID, AppSecret, authorizeUrl, redirectUri, or token values to frontend env files.
